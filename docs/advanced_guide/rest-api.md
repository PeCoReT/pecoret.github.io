---
sidebar_position: 20
---

# REST-API

You can create an API-Token for a project using the frontend.


The token can be used like this:

```bash
curl -H "Authorization: ProjectToken <yourtoken>" http://localhost:8000/projects/1
```
